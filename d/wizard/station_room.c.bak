// Code of ShenZhou
// /d/wizard/station.c
// by aln 4 / 98

inherit ROOM;

void create()
{
        set("short", "中转站");
        set("long", @LONG
这里是【神州新站】
【神州新站】
【神州新站】
【神州新站】
【神州】文件中转站。老远就可以看到这儿排着长队，一群
巫师拿着文件袋在耐心等待。有人在中转站的橱窗后面不停地忙碌着，
时而发出吆喝声，但很快被队伍中叽叽喳喳的吵闹声淹没了。
LONG );

        set("exits", ([
        "up" : __DIR__"center_room",
		"down" : __DIR__"meeting_room" ]));
		

        set("valid_startroom", 1);
        set("no_fight", "1");
        set("no_clean_up", 0);
        set("cost", 0);
        setup();

//        call_other("/clone/board/progress_c", "???");
//      replace_program(ROOM);
}
void init()
{
        object me = this_player();

        if (wizhood(me) == "(player)")
                me->move("/d/death/death.c");
}
