//ROOM: /d/shaolin/shandao.c
// jiuer 08/2001

inherit ROOM;

void create()
{
	set("short", "山道");
	set("long", @LONG
走了一个时辰，你面前出现了一条蜿蜒的山道，山道碎石铺地，
道边树木苍郁，前面隐约可以听到潺潺的溪水声。
LONG );
	set("exits", ([ /* sizeof() == 3 */
		"southeast" : __DIR__"shanlu1",
		"westup" : "/d/jueqing/river",
		]));
	set("cost", 1);
	
	set("outdoors", "shaolin");

	setup();
	replace_program(ROOM);
}