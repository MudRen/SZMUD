// workRoom.c

#include <ansi.h>
inherit ROOM;
void create()
{
    set("short", HIW"思念茅庐"NOR);
 set("long",

     "这里四面环山，山上还隐约可见飘忽的云彩，实在是令人们心旷神怡\n"
     "思念茅庐便是坐落在山腰附近，边上还有一池的荷花，每当夏季来临的时\n"
     "候，这池塘就更是生机勃勃，走到庄前到处鲜花盛开，时不时还有几只蝴\n"
     "蝶经过于此，每个住过这里的人都忍不住赞叹道：人间绝境！\n"
        
NOR);
      set("no_clean_up", 0);
      set("exits", ([
         
     "down" : "/d/city/kedian",
     "wr" : "/d/wizard/wizard_room",

]));
	call_other("/clone/board/miss_b", "???"); 
        setup();
}

void init()
{
        if(base_name(this_object()) == "/u/missing/tangyuan") call_out("destroy", 20, this_object());
}

void destroy(object ob)
{
        destruct(ob);
}
